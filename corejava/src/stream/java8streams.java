package stream;

public class java8streams {

	public static void main(String[] args) {
		// TODO Auto-generated method stub
		List<string>strList = Array.asList("abc","","bcd","","defg","jk");
		long count = strLisy.stream()
				.filter(x -> x.isEmpty())
				.count();
		System.out.println("List is has id strings of length more then %n",strList,num);
		//Remove all empty String from List
		List<String> filtered=strList.stream()
				.filter(x ->)

	}

}
